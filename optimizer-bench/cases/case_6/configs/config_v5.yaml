# Config v5 — active config for case_6
model:
  vocab_size: 8192
  d_model: 512
  n_heads: 8
  text_layers: 4
  num_classes: 50

training:
  batch_size: 16
  epochs: 5
  lr: 5.0e-4
  log_interval: 20

data:
  num_samples: 2000
  text_len: 64
  image_size: 224
  num_workers: 4
