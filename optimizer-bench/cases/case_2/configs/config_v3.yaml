# Config v3 — active config for case_2
model:
  num_classes: 10

training:
  batch_size: 32
  epochs: 5
  lr: 1.0e-3
  accum_steps: 4
  log_interval: 20

data:
  num_samples: 2000
  num_workers: 4
  pin_memory: true

distributed:
  backend: nccl
