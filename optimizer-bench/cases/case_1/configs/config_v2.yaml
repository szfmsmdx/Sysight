# Config v2 — active config for case_1
model:
  vocab_size: 8192
  d_model: 512
  n_heads: 8
  n_layers: 6
  d_ff: 2048
  max_seq_len: 256
  dropout: 0.1

training:
  batch_size: 16
  epochs: 3
  lr: 3.0e-4
  log_interval: 10

data:
  num_samples: 2000
  num_workers: 4
  pin_memory: true
